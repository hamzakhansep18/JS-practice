let age=20;
let accessGranted=(age==18)? 'ture':'false';
console.log(accessGranted);

let year=15;
let message= (year>20) ? 'hey buddy': (year>18) ? 'hey youngster': (year<18) ? 'hello kid':'what unusal message';
console.log(message);

// program to find greater number using conditional op ?
 let num=12;
 let numb=2;
  let greaterNumber=(num>numb) ? `${num} is greater than ${numb}` : `${numb} is greater than ${num}`;
  console.log(greaterNumber)
